#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int ch;
	int CH;

	for (ch = 'a'; ch <= 'z'; CH++)
	{
	putchar(CH);

	}
	for (CH = 'A'; CH <= 'Z'; CH++)
	{
	putchar('\n');
	}

	return (0);
}

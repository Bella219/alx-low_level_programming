#include "main.h"

/**
 * mul - multiplies two numbers
 * @c: tested character
 * Return: 1 if it is, 0 otherwise
 */

int _isdigit(int c)
{
	if ((c >= 48) && (c <= 57))
	{
		return (i);
	}

	return (0);
}

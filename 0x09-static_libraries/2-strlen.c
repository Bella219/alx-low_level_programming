#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @str: string whose length we want to get
 *
 * Return: Lenght of @str.
 */

int _strlen(char *s)
{
	return (strlen(s));
}
